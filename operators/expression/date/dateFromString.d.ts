import { Options } from '../../../core';
/**
 * Converts a date/time string to a date object.
 * @param obj
 * @param expr
 */
export declare function $dateFromString(obj: object, expr: any, options: Options): any;
