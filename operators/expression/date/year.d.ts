import { Options } from '../../../core';
/**
 * Returns the year for a date as a number (e.g. 2014).
 * @param obj
 * @param expr
 */
export declare function $year(obj: object, expr: any, options: Options): number;
