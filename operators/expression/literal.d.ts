import { Options } from "../../core";
/**
 * Return a value without parsing.
 * @param obj
 * @param expr
 * @param options
 */
export declare function $literal(obj: object, expr: any, options: Options): any;
