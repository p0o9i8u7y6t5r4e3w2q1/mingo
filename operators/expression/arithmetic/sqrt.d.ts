import { Options } from '../../../core';
/**
 * Calculates the square root of a positive number and returns the result as a double.
 *
 * @param obj
 * @param expr
 * @returns {number}
 */
export declare function $sqrt(obj: object, expr: any, options: Options): number | null;
