import { Options } from '../../../core';
/**
 * Calculates the log base 10 of a number and returns the result as a double.
 *
 * @param obj
 * @param expr
 * @returns {number}
 */
export declare function $log10(obj: object, expr: any, options: Options): number | null;
