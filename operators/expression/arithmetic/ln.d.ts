import { Options } from '../../../core';
/**
 * Calculates the natural logarithm ln (i.e loge) of a number and returns the result as a double.
 *
 * @param obj
 * @param expr
 * @returns {number}
 */
export declare function $ln(obj: object, expr: any, options: Options): number | null;
