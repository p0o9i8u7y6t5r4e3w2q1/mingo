import { Options } from '../../../core';
/**
 * Raises Euler’s number (i.e. e ) to the specified exponent and returns the result.
 *
 * @param obj
 * @param expr
 * @returns {number}
 */
export declare function $exp(obj: object, expr: any, options: Options): number | null;
