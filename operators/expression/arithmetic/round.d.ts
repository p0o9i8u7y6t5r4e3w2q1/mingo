import { Options } from '../../../core';
/**
 * Rounds a number to to a whole integer or to a specified decimal place.
 * @param {*} obj
 * @param {*} expr
 */
export declare function $round(obj: object, expr: any, options: Options): number | null;
