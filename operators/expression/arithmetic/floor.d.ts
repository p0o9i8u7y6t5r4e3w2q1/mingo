import { Options } from '../../../core';
/**
 * Returns the largest integer less than or equal to the specified number.
 *
 * @param obj
 * @param expr
 * @returns {number}
 */
export declare function $floor(obj: object, expr: any, options: Options): number | null;
